<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.keyesit</groupId>
  <artifactId>msgraph-cli</artifactId>
  <version>1.0.0</version>
  <name>msgraph-cli</name>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Verified on Maven Central -->
    <msgraph.version>6.60.0</msgraph.version>
    <!-- Pick a recent Azure Identity (can be bumped as needed) -->
    <azure.identity.version>1.16.3</azure.identity.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.microsoft.graph</groupId>
      <artifactId>microsoft-graph</artifactId>
      <version>${msgraph.version}</version>
    </dependency>

    <dependency>
      <groupId>com.azure</groupId>
      <artifactId>azure-identity</artifactId>
      <version>${azure.identity.version}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>1.5.16</version>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <!-- Run main class via: mvn -q exec:java -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <mainClass>com.keyesit.graphcli.GraphCli</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>

